---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - clip
    - bookmarks
permalink: /tags/{{ tag }}/
layout: layouts/base.njk
title: "Tagged “{{ tag }}”"
metaDescription: "All posts from the Blog tagged with “{{ tag or eleventyComputed.title }}”"
---

<h1>Tagged “{{ tag or eleventyComputed.title}}”</h1>

{% set postslist = collections[tag] %}

<ol>
{% set taglist = collections[ tag ] %}
{% for post in taglist | reverse %}
  <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
{% endfor %}
</ol>

<nav>
  <a href="{{ '/writing/' | url }}">← Blog index</a>
</nav>
