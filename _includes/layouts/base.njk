<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body class="{{ section }}">

		<header>
			<div class="logo">
					<a href="{{ '/' | url }}"><img src="/static/img/ampersam.svg" alt="{{ metadata.title }}" /></a><span> Sameera Kapila</span>
			</div>
			{% include "components/nav.njk" %}
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>

		{% include "components/footer.njk" %}

	</body>

</html>
